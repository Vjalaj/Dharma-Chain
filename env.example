# Google OAuth
# Documentation: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# NextAuth
# Generate a secret with `openssl rand -base64 32` in your terminal
# This is required for NextAuth to encrypt sessions and JWT tokens
NEXTAUTH_SECRET=""
# Your application's base URL (change to your domain in production)
NEXTAUTH_URL="http://localhost:3000"

# Firebase
# Find these in your Firebase project settings (Project settings > General > Your apps > SDK setup and configuration)
# Note: These keys are safe to be public. Security is handled by Firestore rules, not by keeping these secret.
NEXT_PUBLIC_FIREBASE_API_KEY=""
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=""
NEXT_PUBLIC_FIREBASE_PROJECT_ID=""
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=""
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=""
NEXT_PUBLIC_FIREBASE_APP_ID=""
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=""

# Razorpay
# Find these in your Razorpay dashboard (Settings > API Keys)
RAZORPAY_KEY_ID=""
RAZORPAY_KEY_SECRET=""

# Admin Configuration
# Add admin email addresses separated by commas (for admin panel access)
ADMIN_EMAILS=""
